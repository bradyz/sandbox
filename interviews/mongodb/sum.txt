arr = [-1, 5, 3, 2]
sorted_arr = [-1, 2, 3, 5]

s = 4

for x in range(len(arr)):
  for y in range(len(arr)):
    if x != y and arr[x] + arr[y] == s
    return x, y

    2 3 = 5
    -1 3 = 2
    -1 5 = 4

    2 3 = 5
    2 5 = 7

    right = len(arr) / 2
    left = x - 1

    while x < len(arr) and y < len(arr) and x > 0 and y > 0:
    xy = arr[x] + arr[y]
    if xy < s:
    y += 1
    elif 
